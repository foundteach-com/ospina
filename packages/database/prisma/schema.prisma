generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Modelo de ejemplo - ajustar según necesidades del negocio
model User {
  id        String   @id @default(cuid())
  email     String   @unique
  password  String
  name      String?
  role      UserRole @default(USER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  orders    Order[]
}

enum UserRole {
  USER
  ADMIN
  EMPLOYEE
}

model Product {
  id          String      @id @default(cuid())
  name        String
  description String
  price       Decimal
  sku         String      @unique
  stock       Int
  images      String[]
  categoryId  String?
  category    Category?   @relation(fields: [categoryId], references: [id])
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  OrderItem   OrderItem[]
}

model Category {
  id       String    @id @default(cuid())
  name     String
  products Product[]
}

model Order {
  id        String      @id @default(cuid())
  userId    String
  user      User        @relation(fields: [userId], references: [id])
  total     Decimal
  status    OrderStatus @default(PENDING)
  items     OrderItem[]
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
}

model OrderItem {
  id        String  @id @default(cuid())
  orderId   String
  order     Order   @relation(fields: [orderId], references: [id])
  productId String
  product   Product @relation(fields: [productId], references: [id])
  quantity  Int
  price     Decimal
}

enum OrderStatus {
  PENDING
  PROCESSING
  SHIPPED
  DELIVERED
  CANCELLED
}

model CashFlow {
  id            String       @id @default(cuid())
  date          DateTime
  receiptNumber String       // Numero de recibo del comprobante
  provider      String       // Proveedor
  description   String       // Descripcion del registro
  type          CashFlowType // Ingreso o Gasto
  amount        Decimal      // Monto
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
}

enum CashFlowType {
  INCOME  // Ingreso
  EXPENSE // Gasto
}
